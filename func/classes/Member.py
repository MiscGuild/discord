# The following file contains: mute, unmute, kick, ban, softban, unban, forcesync, add, remove, accept

import discord

class Member:
    def __init__(self, member: discord.Member):
        self.member = member

    # async def mute(reason: str=None):

    # async def unmute():

    # async def kick(reason: str=None):

    # async def ban(reason: str=None):

    # async def softban(reason: str=None):

    # async def unban():

    # async def forcesync(new_name: str):

    # async def add():

    # async def remove():

    # async def accept():
    
# The following file contains: mute, unmute, kick, ban, softban, unban, forcesync, add, remove, accept

import discord

class Member:
    def __init__(self, member: discord.Member):
        self.member = member

    # async def mute(reason=None):

    # async def unmute():

    # async def kick(reason=None):

    # async def ban(reason=None):

    # async def softban(reason=None):

    # async def unban():

    # async def forcesync(new_name):

    # async def add():

    # async def remove():

    # async def accept():
    